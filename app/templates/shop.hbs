<div class="container">
  <div class="row">
    <div class="col-xs-9">
      {{#each products as |product index|}}
        {{product-item product=product}}
      {{/each}}
    </div>

    <div class="col-xs-3">
      {{show-cart}}
    </div>
  </div>
</div>

<div id="payment-section" class="container">
  <div class="row">
    This is where checkout form will be
    {{payment-form}}
    {{payment-responses}}
  </div>
</div>

{{outlet}}
